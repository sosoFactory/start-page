# PRD: 데스크톱 브라우저 시작 페이지 (Desktop Browser Start Page)

## 1. 개요 및 목표
데스크톱 브라우저를 켰을 때 **자주 가는 링크(62% 메인)** 를 중심으로, **일산 등 시/군/구 상세 지역의 오늘/내일 날씨 및 강수확률** 과 **7대 핵심 시장 시세(KOSPI, KOSDAQ, S&P 500, NASDAQ, TLT, GOLD, BTC)** 를 한눈에 볼 수 있는 깔끔하고 정갈한 개인화 대시보드를 구축합니다.

---

## 2. 기능 요구사항: 확장 프로그램 툴바 팝업 내 북마크 중복 감지 및 안내 UI

### 2.1 요구사항 배경 및 목적
- 이미 시작페이지 즐겨찾기에 등록된 웹사이트에서 크롬 확장 프로그램 별 아이콘을 클릭했을 때, 중복 등록을 방지하고 사용자에게 이미 등록되어 있음을 명확하게 안내합니다.

### 2.2 상세 기능 및 UI/UX 사양
1. **URL 정규화 및 중복 검사 로직 (`popup.js`)**:
   - 팝업 오픈 시 `chrome.tabs.query`로 현재 활성 탭 URL 획득.
   - `chrome.storage.local.get('saniti_links_v1')`를 통해 현재 저장된 북마크 목록 조회 (비어있을 경우 기본 5개 프리셋 기준).
   - 프로토콜(`http://`, `https://`), 서브도메인(`www.`), 마지막 슬래시(`/`)를 제거한 정규화 URL 기준으로 기존 북마크와 일치 여부 비교.
2. **이미 등록된 경우의 화면 처리 (`popup.html`, `popup.css`)**:
   - 입력 폼 대신 **"⭐ 이미 즐겨찾기에 등록된 사이트입니다"** 알림 카드 노출.
   - 기존에 등록되어 있는 사이트 제목 표시.
   - 하단에 간결한 `[확인 / 닫기]` 버튼 제공.
3. **등록되지 않은 경우의 화면 처리**:
   - 기존의 신규 북마크 추가 폼(URL, 제목 수정, `[추가하기]`, `[취소]`) 정상 노출.

---

## 3. 진행 계획
1. **PRD 작성 및 사용자 승인** (현재 단계)
2. **`public/popup.html`**: 중복 안내 카드 레이아웃 및 스타일 추가
3. **`public/popup.js`**: 스토리지 조회, URL 정규화 비교 및 중복 분기 처리 구현
4. **빌드 및 동작 검증**: `npm test` 및 `npm run build` 검증 
