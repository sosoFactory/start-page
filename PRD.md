# PRD: 데스크톱 브라우저 시작 페이지 (Desktop Browser Start Page)

## 1. 개요 및 목표
데스크톱 브라우저를 켰을 때 **자주 가는 링크(62% 메인)** 를 중심으로, **일산 등 시/군/구 상세 지역의 오늘/내일 날씨 및 강수확률** 과 **7대 핵심 시장 시세(KOSPI, KOSDAQ, S&P 500, NASDAQ, TLT, GOLD, BTC)** 를 한눈에 볼 수 있는 깔끔하고 정갈한 개인화 대시보드를 구축합니다.

---

## 2. 변경 요구사항: 스토리지 레이스 컨디션 방어 및 프리셋 간소화 (효율적 아키텍처)

### 2.1 요구사항 배경 및 목적
- 시작페이지(새 탭)가 닫혀있을 때 툴바 팝업으로 추가한 새 링크가 새 탭을 열 때 옛날 상태로 덮어씌워져 증발하는 버그를 완벽히 해결.
- 불필요하게 비대한 기본 링크 목록을 실용적인 대표 5개(YouTube, GitHub, ChatGPT, Naver, Google)로 가볍게 압축하고, 스토리지 동기화 코드를 가장 견고하고 효율적인 구조로 리팩토링.

### 2.2 상세 설계 및 아키텍처
1. **스토리지 하이드레이션 가드 (`useLocalStorage.ts`)**:
   - `isHydratedRef` 플래그를 도입하여, 컴포넌트 마운트 시 `chrome.storage.local`의 최신 데이터를 완벽하게 읽어오기 전까지는 `chrome.storage.local.set` 저장을 차단.
   - 툴바 팝업이 백그라운드에서 저장해 둔 새 링크를 최우선으로 복원하여 화면과 `localStorage`에 즉시 반영.
2. **기본 프리셋 링크 5개 간소화 (`presetLinks.ts` & `popup.js`)**:
   - YouTube, GitHub, ChatGPT, Naver, Google 5대 핵심 사이트로 가볍게 정리하여 데이터 일원화.
   - 팝업과 대시보드 간 스토리지 참조 키(`saniti_links_v1`) 및 데이터 구조 완전 일치.

---

## 3. 진행 절차
1. **PRD 작성** (현재 단계 - 오직 PRD.md만 작성)
2. **사용자 승인 확인** (대기)
3. **`useLocalStorage.ts` 하이드레이션 가드 구현** 
4. **`presetLinks.ts` 및 `popup.js` 5개 대표 프리셋으로 간소화** 
5. **단위 테스트 및 빌드 검증** 
