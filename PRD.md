# PRD: 데스크톱 브라우저 시작 페이지 (Desktop Browser Start Page)

## 1. 개요 및 목표
데스크톱 브라우저를 켰을 때 **자주 가는 링크(62% 메인)** 를 중심으로, **일산 등 시/군/구 상세 지역의 오늘/내일 날씨 및 강수확률** 과 **7대 핵심 시장 시세(KOSPI, KOSDAQ, S&P 500, NASDAQ, TLT, GOLD, BTC)** 를 한눈에 볼 수 있는 깔끔하고 정갈한 개인화 대시보드를 구축합니다.

---

## 2. 신규 기능 요구사항: 바로가기 링크 JSON 내보내기(Export) 및 가져오기(Import)

### 2.1 요구사항 배경 및 목적
- 사용자가 기기를 변경하거나 브라우저 캐시를 삭제했을 때 북마크 데이터가 유실되지 않도록 보호하고, 손쉽게 백업/복원할 수 있는 **JSON 파일 기반의 내보내기/가져오기 기능** 을 제공합니다.

### 2.2 상세 기능 및 UI/UX 사양
1. **버튼 배치 및 스타일 (`LinksHub.tsx`, `app.css`)**:
   - `자주 가는 링크` 카드 헤더 우측의 `[+ 바로가기 추가]` 버튼 **왼쪽** 에 배치.
   - **내보내기 버튼 (`[⬇ 내보내기]`)**: `Download` 아이콘 + 텍스트 버튼 (`btn-secondary` 스타일).
   - **가져오기 버튼 (`[⬆ 가져오기]`)**: `Upload` 아이콘 + 텍스트 버튼 (`btn-secondary` 스타일).
2. **내보내기 (Export) 동작**:
   - 클릭 시 현재 링크 목록(`BookmarkLink[]`)을 UTF-8 JSON 문자열로 변환.
   - `startpage-bookmarks-YYYYMMDD.json` 파일명으로 브라우저 즉시 다운로드 실행 (`Blob` + `URL.createObjectURL`).
3. **가져오기 (Import) 동작**:
   - 클릭 시 숨겨진 `<input type="file" accept=".json" />` 파일 선택 창 오픈.
   - 선택된 JSON 파일 파싱 및 스키마 유효성 검사 (`id`, `title`, `url` 필수 검증).
   - 유효하지 않은 파일 업로드 시 에러 알림.
   - 유효한 파일일 경우 **"기존 북마크를 백업 파일의 링크(N개)로 교체하시겠습니까?"** 확인 후 스토리지 및 화면 즉시 갱신.

---

## 3. 진행 절차
1. **PRD 작성** (현재 단계 - 오직 PRD.md만 작성)
2. **사용자 승인 확인** (대기)
3. **`LinksHub.tsx`에 내보내기/가져오기 핸들러 및 버튼 UI 구현** 
4. **`app.css` 헤더 액션 버튼 스타일링** 
5. **단위 테스트 및 빌드 검증** 
