# PRD: 데스크톱 브라우저 시작 페이지 (Startpage Dashboard)

## 1. 제품 개요 및 비전 (Overview & Vision)

### 1.1 제품 정의
데스크톱 브라우저를 열었을 때 **자주 가는 링크(62% 메인 허브)** 를 중심으로, **상세 지역의 오늘/내일 날씨 및 시간대별 강수확률** 과 **7대 핵심 시장 시세(국내/해외/가상자산)** 를 한눈에 파악할 수 있는 깔끔하고 정갈한 개인화 대시보드입니다.

### 1.2 핵심 가치
- **0초 시작 (Zero Delay)**: 불필요한 네트워크 대기 없이 로컬 퍼스트(Local-First)로 즉각 로드되는 초경량 대시보드.
- **100vh 무스크롤 압축 뷰포트**: 한 화면에 모든 필수 정보를 밀도 있게 배치하여 스크롤 스트레스 제거.
- **Saniti 미니멀 디자인**: 정갈한 Pretendard(프리텐다드) 타이포그래피와 코랄 포인트 컬러(`#f36458`) 기반의 편안하고 세련된 시각 경험.
- **100% 로컬 데이터 보안**: 모든 북마크 및 설정 데이터를 외부 서버에 전송하지 않고 브라우저 로컬(`localStorage` / `chrome.storage`)에만 안전하게 보관.

---

## 2. 시스템 아키텍처 및 지원 환경

### 2.1 기술 스택
- **Frontend Framework**: React 18, TypeScript, Vite
- **Typography**: Pretendard (가변/웹폰트 단일화, 불필요한 모노폰트 제거)
- **Data Visualization**: Recharts (강수확률 바 차트, 시장 등락 곡선)
- **Icons**: Lucide React
- **Testing**: Vitest, React Testing Library (TDD 원칙 준수)

### 2.2 실행 및 배포 형태
1. **GitHub Pages 웹 애플리케이션**: URL(`https://sosofactory.github.io/start-page/`) 접속을 통한 브라우저 홈/시작 페이지 등록.
2. **크롬 확장 프로그램 (Manifest V3)**:
   - `chrome_url_overrides.newtab`: 새 탭(`Ctrl + T`) 오픈 시 대시보드 즉시 로드.
   - `action.default_popup`: 브라우저 툴바 별(⭐) 아이콘 클릭 시 현재 페이지를 1초 만에 바로가기에 추가하는 미니 팝업.

---

## 3. 핵심 기능 상세 명세 (Functional Requirements)

### 3.1 🔗 자주 가는 링크 허브 (Main Section - 62% 비중)
1. **그리드 레이아웃**:
   - 5열 반응형 카드 그리드로 바로가기 타일 배치.
   - 각 타일은 파비콘, 볼드(Bold) 도메인 호스트, 은은한 세부 경로, 수정/삭제 버튼으로 구성.
   - 클릭 시 새 창이 아닌 현재 탭에서 지연 없이 즉시 탐색.
2. **HTML5 네이티브 드래그 앤 드롭 (Drag & Drop Reorder)**:
   - 타일을 마우스로 끌어서 자유롭게 순서 재배치 가능.
   - 배열 인덱스 순서 그대로 로컬 스토리지에 영속 저장.
3. **파비콘 로딩 회복성 (Favicon Fallback)**:
   - Google S2 Favicon 서비스 연동 (`https://www.google.com/s2/favicons?domain=...`).
   - 파비콘 로딩 실패 또는 네트워크 미연결 시 지구본/이니셜 배지로 자동 전환.
4. **바로가기 관리 모달 (`BookmarkModal`)**:
   - URL 입력 시 프로토콜(`https://`) 자동 정규화 및 사이트명 자동 파싱.
   - 추가 및 수정 시 유효성 검사 및 실시간 반영.
5. **실시간 인라인 검색 및 필터링 (Instant Search & Filter)**:
   - 별도의 검색 버튼 없이 검색창에 키워드 입력 즉시 실시간 필터링.
   - `[+ 바로가기 추가]` 버튼과 높이(`30px`)를 완벽히 일치시키고 선명한 테두리와 확장 너비로 가시성 대폭 강화.
   - 검색어 입력 시 우측 원클릭 지우기(`X`) 버튼 및 키보드 `ESC` 키 초기화 지원.
   - 검색 결과 없음 빈 상태 UI 및 검색 중 드래그 정렬 잠금 보호.

### 3.2 ⚙️ 공통 모달 시스템 (`Modal.tsx`) 및 종합 설정 모달 (`SettingsModal`)
1. **공통 기반 모달 컴포넌트 (`Modal.tsx`)**:
   - 대시보드의 모든 모달(`BookmarkModal`, `RegionSelectModal`, `SettingsModal`)을 단일 공통 모달 컴포넌트로 일원화.
   - `ESC` 키보드 닫기 이벤트, 오버레이 배경 클릭 방어, 일관된 헤더(아이콘 + 제목 + 닫기 버튼), 본문, 푸터 템플릿 제공.
2. **자연스러운 모달 팝업 애니메이션 (`app.css`)**:
   - **오버레이**: 부드러운 페이드인 (`modalOverlayFadeIn 0.18s ease-out`).
   - **다이얼로그**: 부드러운 팝업 & 스케일 (`modalDialogPopIn 0.22s cubic-bezier(0.16, 1, 0.3, 1)`: `scale(0.95) -> scale(1)`).
3. **헤더 설정 버튼 (`App.tsx`)**:
   - 우측 상단 설정 버튼에 아이콘(`Settings`)과 함께 직관적인 **'설정'** 텍스트를 나란히 표기하여 버튼 기능 명확화.
4. **모달 레이아웃 및 헤더 정렬**:
   - 모달 헤더 좌측의 아이콘과 제목 간 `flex` 수평 중앙 정렬 및 여백 확보로 시각적 깨짐 방지.
   - 모달 내부 데이터 관리 행에서 내보내기/가져오기/초기화 버튼 영역이 텍스트 길이에 밀리거나 줄바꿈되어 깨지지 않도록 `flex-shrink: 0`, `white-space: nowrap` 및 일관된 보조 버튼 스타일(`btn-secondary`, `btn-danger-outline`) 보장.
3. **일반 설정 (General Settings)**:
   - **링크 열기 방식**: `현재 탭 (_self, 기본)` vs `새 탭 (_blank)` 선택 지원.
   - **헤더 시계 표시**: 시계 위젯 표시 토글 (ON / OFF, 기본 ON).
   - **시간 표기 방식**: `24시간제 (예: 14:30)` vs `12시간제 (예: 오후 2:30)` 선택.
   - **초(Seconds) 단위 표시**: `14:30:15` vs `14:30` 토글.
3. **데이터 관리 (Data Management)**:
   - **북마크 백업 (JSON 내보내기)**: `startpage-bookmarks-YYYYMMDD.json` 파일 생성 및 즉시 다운로드.
   - **북마크 복원 (JSON 가져오기)**: 파일 선택 및 스키마 검증 후 일괄 복원.
   - **기본값 초기화**: 확인 대화상자(`window.confirm`) 후 기본 대표 5개 링크로 원클릭 리셋.
4. **도움말 & 정보 (About & Help)**:
   - 브라우저 시작 페이지 설정 가이드 (웹 주소 / 확장 프로그램 등록).
   - 툴바 별(⭐) 아이콘 미니 팝업 1초 등록 활용 팁.
   - 100% 로컬 스토리지 데이터 보안 안내.

### 3.3 🕒 헤더 실시간 디지털 시계 (`HeaderClock`)
1. **타이포그래피 및 무박스 개방형 디자인 (Borderless & Large Typography)**:
   - 배경 박스(`background`, `border`, `padding`)를 제거하여 답답한 박스에 갇히지 않고 헤더에 자연스럽게 녹아드는 미니멀 타이포그래피 적용.
   - 시간 텍스트 크기를 대폭 확대(`16px`~`18px`, Bold, `tabular-nums`)하여 한눈에 시간을 명확히 확인할 수 있도록 가시성 극대화.
   - 날짜(`9월 8일 (화)`)는 은은한 슬레이트 톤(`12.5px`)으로 보조 표기하여 시각적 위계 확립.
2. **실시간 렌더링 및 설정 동기화**:
   - 1초 간격 실시간 갱신.
   - `DashboardSettings` 설정에 따라 12h/24h 포맷 및 초(Seconds) 표시 동적 반영.

### 3.4 ⭐ 크롬 확장 프로그램 통합 (Extension Integration)
1. **툴바 별(⭐) 아이콘 미니 팝업 (`popup.html`, `popup.js`)**:
   - 웹서핑 중 툴바 별 아이콘 클릭 시 현재 탭의 URL 및 페이지 제목이 자동 로드됨.
2. **북마크 중복 감지 및 상태 분기**:
   - 팝업 로드 시 저장된 북마크 목록과 현재 탭 URL을 정규화(`http(s)://`, `www.`, 말미 슬래시 제거)하여 대조.
   - **이미 등록된 사이트**: 추가 폼 대신 **"⭐ 이미 등록된 바로가기입니다"** 알림 카드와 기존 등록명을 표시하고 `[확인]` 버튼 제공.
   - **신규 사이트**: 사이트명 수정 및 `[추가하기]` 폼 노출.
3. **무중단 실시간 동기화 (`useLocalStorage.ts`)**:
   - `isHydratedRef` 하이드레이션 가드를 통해 시작 페이지가 닫혀있거나 여러 탭이 열려있어도 스토리지 덮어쓰기 없이 실시간 동기화 보장.

### 3.5 🌦️ 날씨 & 강수확률 예보 위젯 (Sidebar - 상단)
1. **상세 지역 날씨 연동**:
   - 대한민국 기상청 단기예보 및 Open-Meteo API 연동.
   - 전국 250+ 시/군/구 및 3,800+ 읍면동 행정구역 검색 및 선택 지원 (기본값: 경기도 고양시 일산동구).
2. **오늘 & 내일 대표 요약**:
   - 직관적인 날씨 아이콘, 실시간 강수확률(%), 최저/최고 기온 명문화 (`최저 18° · 최고 26°`).
3. **48시간 시간대별 강수 바 차트**:
   - 2시간 단위 시간대별 강수확률 시각화 및 현재 시간대 하이라이트.

### 3.6 📈 7대 주요 시장 시세 위젯 (Sidebar - 하단)
1. **7대 핵심 자산 실시간 등락**:
   - 국내 시장: KOSPI, KOSDAQ
   - 해외 시장: S&P 500, NASDAQ, TLT (미국 20년 국채)
   - 원자재 및 가상자산: GOLD, BTC
2. **시장별 운영 시간 분기**:
   - 한국 정규장(09:00 - 15:30), 미국 정규장(09:30 - 16:00), 24시간 가상자산 처리.
3. **동적 트렌드 곡선 (Wave Chart)**:
   - 각 자산의 당일 변동폭에 최적화된 동적 Y축 스케일링으로 등락 추세 시각화.

### 3.7 💬 인터랙션 & 툴팁 UI 디자인 원칙 (Interaction & Tooltip Standards)
1. **브라우저 기본 `title` 속성 배제**:
   - `title` 속성을 브라우저 툴팁 도구로 사용하지 않으며, 일관된 스타일의 커스텀 CSS 툴팁(`[data-tooltip]`) 시스템을 구축하여 적용.
2. **아이콘 전용 버튼 툴팁 필수화**:
   - 텍스트 없이 아이콘만 존재하는 버튼(예: 날씨/시세 새로고침, 북마크 수정/삭제, 검색어 지우기, 모달 닫기 등)은 호버 시 기능명을 명확히 안내하는 커스텀 툴팁 제공.
   - 텍스트가 함께 표기된 버튼(예: '설정', '바로가기 추가')은 툴팁을 생략하여 시각적 간결성 유지.
3. **카드/모달 상단 오버플로 클리핑 방지 포지셔닝 (`data-tooltip-pos`)**:
   - 카드 헤더나 모달 상단처럼 `overflow: hidden`이 적용된 컨테이너 내부의 아이콘 버튼은 상단 대신 하단 또는 좌하단(`bottom`, `bottom-left`) 방향으로 툴팁을 띄워 텍스트가 가려지거나 잘리지 않도록 보장.

---

## 4. 데이터 모델 및 스토리지 명세

### 4.1 북마크 데이터 (`BookmarkLink`)
```typescript
export interface BookmarkLink {
  id: string;     // 고유 식별자 (예: '1', 'link-1725500000000')
  title: string;  // 사이트 표시 이름
  url: string;    // 대상 웹사이트 전체 URL
}
```

### 4.2 대시보드 환경설정 (`DashboardSettings`)
```typescript
export interface DashboardSettings {
  openInNewTab: boolean;        // 링크 새 탭 열기 여부 (기본: false)
  showClock: boolean;           // 헤더 시계 표시 여부 (기본: true)
  clockFormat: '24h' | '12h';   // 시간 포맷 (기본: '24h')
  showClockSeconds: boolean;    // 초 단위 표시 여부 (기본: true)
}
```

### 4.3 스토리지 키 관리
- `saniti_links_v1`: 북마크 링크 배열 (`BookmarkLink[]`)
- `saniti_settings_v1`: 대시보드 환경설정 (`DashboardSettings`)
- `saniti_region_v1`: 선택된 날씨 지역 정보 (`Region`)

---

## 5. 향후 로드맵 (Future Roadmap)

- [x] **실시간 디지털 시계 위젯 (`HeaderClock`)**
- [x] **종합 설정 및 가이드 모달 (`SettingsModal`)**
- [x] **북마크 데이터 백업/복원 (JSON Export/Import)**
- [x] **전역 폰트 Pretendard 단일화 및 모노폰트 삭제**
- [x] **모던 커스텀 툴팁 시스템 (`[data-tooltip]`)**
- [x] **공통 모달 컴포넌트 및 자연스러운 애니메이션 (`Modal.tsx`)**
- [ ] **단축키 지원**: `?` 또는 `Ctrl+/` 키로 설정 모달 열기, `/` 키로 검색창 포커스
- [ ] **테마 개인화**: 다크 모드 및 커스텀 배경 옵션
- [ ] **날씨 위젯 다중 지역 즐겨찾기 탭 전환**
 
