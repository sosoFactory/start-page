# PRD: 데스크톱 브라우저 시작 페이지 (Startpage Dashboard)

## 1. 제품 개요 및 비전 (Overview & Vision)

### 1.1 제품 정의
데스크톱 브라우저를 열었을 때 **자주 가는 링크(62% 메인 허브)** 를 중심으로, **상세 지역의 오늘/내일 날씨 및 시간대별 강수확률** 과 **7대 핵심 시장 시세(국내/해외/가상자산)** 를 한눈에 파악할 수 있는 깔끔하고 정갈한 개인화 대시보드입니다.

### 1.2 핵심 가치
- **0초 시작 (Zero Delay)**: 불필요한 네트워크 대기 없이 로컬 퍼스트(Local-First)로 즉각 로드되는 초경량 대시보드.
- **100vh 무스크롤 압축 뷰포트**: 한 화면에 모든 필수 정보를 밀도 있게 배치하여 스크롤 스트레스 제거.
- **Saniti 미니멀 디자인**: 정갈한 Noto Sans KR 타이포그래피와 코랄 포인트 컬러(`#f36458`) 기반의 편안한 시각 경험.
- **100% 로컬 데이터 보안**: 모든 북마크 및 설정 데이터를 외부 서버에 전송하지 않고 브라우저 로컬(`localStorage` / `chrome.storage`)에만 안전하게 보관.

---

## 2. 시스템 아키텍처 및 지원 환경

### 2.1 기술 스택
- **Frontend Framework**: React 18, TypeScript, Vite
- **Data Visualization**: Recharts (강수확률 바 차트, 시장 등락 곡선)
- **Icons**: Lucide React
- **Testing**: Vitest, React Testing Library (TDD 원칙 준수)

### 2.2 실행 및 배포 형태
1. **GitHub Pages 웹 애플리케이션**: URL(`https://haksoo0918.github.io/start-page/`) 접속을 통한 브라우저 홈/시작 페이지 등록.
2. **크롬 확장 프로그램 (Manifest V3)**:
   - `chrome_url_overrides.newtab`: 새 탭(`Ctrl + T`) 오픈 시 대시보드 즉시 로드.
   - `action.default_popup`: 브라우저 툴바 별(⭐) 아이콘 클릭 시 현재 페이지를 1초 만에 바로가기에 추가하는 미니 팝업.

---

## 3. 핵심 기능 상세 명세 (Functional Requirements)

### 3.1 🔗 자주 가는 링크 허브 (Main Section - 62% 비중)
1. **그리드 레이아웃**:
   - 5열 반응형 카드 그리드로 바로가기 타일 배치.
   - 각 타일은 파비콘, 볼드(Bold) 도메인 호스트, 은은한 세부 경로, 수정/삭제 버튼으로 구성.
   - 클릭 시 새 창이 아닌 현재 탭에서 지연 없이 즉시 탐색.
2. **HTML5 네이티브 드래그 앤 드롭 (Drag & Drop Reorder)**:
   - 타일을 마우스로 끌어서 자유롭게 순서 재배치 가능.
   - 배열 인덱스 순서 그대로 로컬 스토리지에 영속 저장.
3. **파비콘 로딩 회복성 (Favicon Fallback)**:
   - Google S2 Favicon 서비스 연동 (`https://www.google.com/s2/favicons?domain=...`).
   - 파비콘 로딩 실패 또는 네트워크 미연결 시 지구본/이니셜 배지로 자동 전환.
4. **바로가기 관리 모달 (`BookmarkModal`)**:
   - URL 입력 시 프로토콜(`https://`) 자동 정규화 및 사이트명 자동 파싱.
   - 추가 및 수정 시 유효성 검사 및 실시간 반영.
5. **실시간 인라인 검색 및 필터링 (Instant Search & Filter)**:
   - 별도의 검색 버튼 없이 검색창에 키워드 입력 즉시 실시간 필터링.
   - `[+ 바로가기 추가]` 버튼과 높이(`30px`)를 완벽히 일치시키고 선명한 테두리와 확장 너비로 가시성 대폭 강화.
   - 검색어 입력 시 우측 원클릭 지우기(`X`) 버튼 및 키보드 `ESC` 키 초기화 지원.
   - 검색 결과 없음 빈 상태 UI 및 검색 중 드래그 정렬 잠금 보호.

### 3.2 ⚙️ 대시보드 우측 상단 설정 드롭다운 (`SettingsDropdown`)
1. **헤더 설정 버튼**:
   - 대시보드 최상단 우측에 톱니바퀴(`Settings`) 아이콘의 정갈한 설정 버튼 배치.
2. **드롭다운 메뉴 기능**:
   - 설정 버튼 클릭 시 토글되는 세련된 팝오버 메뉴.
   - **북마크 JSON 내보내기 (`handleExportJson`)**: 현재 저장된 북마크를 `startpage-bookmarks-YYYYMMDD.json` 파일로 즉시 다운로드 백업.
   - **북마크 JSON 가져오기 (`handleImportJson`)**: 백업 파일 선택 시 유효성 검사 및 확인 대화상자를 거쳐 안전하게 일괄 복원.
   - 외부 클릭 시 자동으로 닫히는 Backdrop/Click-Outside 핸들링.

### 3.3 ⭐ 크롬 확장 프로그램 통합 (Extension Integration)
1. **툴바 별(⭐) 아이콘 미니 팝업 (`popup.html`, `popup.js`)**:
   - 웹서핑 중 툴바 별 아이콘 클릭 시 현재 탭의 URL 및 페이지 제목이 자동 로드됨.
2. **북마크 중복 감지 및 상태 분기**:
   - 팝업 로드 시 저장된 북마크 목록과 현재 탭 URL을 정규화(`http(s)://`, `www.`, 말미 슬래시 제거)하여 대조.
   - **이미 등록된 사이트**: 추가 폼 대신 **"⭐ 이미 등록된 바로가기입니다"** 알림 카드와 기존 등록명을 표시하고 `[확인]` 버튼 제공.
   - **신규 사이트**: 사이트명 수정 및 `[추가하기]` 폼 노출.
3. **무중단 실시간 동기화 (`useLocalStorage.ts`)**:
   - `isHydratedRef` 하이드레이션 가드를 통해 시작 페이지가 닫혀있거나 여러 탭이 열려있어도 스토리지 덮어쓰기 없이 실시간 동기화 보장.

### 3.3 🌦️ 날씨 & 강수확률 예보 위젯 (Sidebar - 상단)
1. **상세 지역 날씨 연동**:
   - 대한민국 기상청 단기예보 및 Open-Meteo API 연동.
   - 전국 250+ 시/군/구 및 3,800+ 읍면동 행정구역 검색 및 선택 지원 (기본값: 경기도 고양시 일산동구).
2. **오늘 & 내일 대표 요약**:
   - 직관적인 날씨 아이콘, 실시간 강수확률(%), 최저/최고 기온 명문화 (`최저 18° · 최고 26°`).
3. **48시간 시간대별 강수 바 차트**:
   - 2시간 단위 시간대별 강수확률 시각화 및 현재 시간대 하이라이트.

### 3.4 📈 7대 주요 시장 시세 위젯 (Sidebar - 하단)
1. **7대 핵심 자산 실시간 등락**:
   - 국내 시장: KOSPI, KOSDAQ
   - 해외 시장: S&P 500, NASDAQ, TLT (미국 20년 국채)
   - 원자재 및 가상자산: GOLD, BTC
2. **시장별 운영 시간 분기**:
   - 한국 정규장(09:00 - 15:30), 미국 정규장(09:30 - 16:00), 24시간 가상자산 처리.
3. **동적 트렌드 곡선 (Wave Chart)**:
   - 각 자산의 당일 변동폭에 최적화된 동적 Y축 스케일링으로 등락 추세 시각화.

---

## 4. 데이터 모델 및 스토리지 명세

### 4.1 북마크 데이터 (`BookmarkLink`)
```typescript
export interface BookmarkLink {
  id: string;     // 고유 식별자 (예: '1', 'link-1725500000000')
  title: string;  // 사이트 표시 이름
  url: string;    // 대상 웹사이트 전체 URL
}
```

### 4.2 스토리지 키 관리
- `saniti_links_v1`: 북마크 링크 배열 (`BookmarkLink[]`)
- `saniti_weather_region_v1`: 선택된 날씨 지역 정보 (`RegionInfo`)
- `saniti_clock_mode_v1`: 시계 표시/포맷 설정 (예정)

---

## 5. 향후 로드맵 (Future Roadmap)

- [ ] **실시간 디지털 시계 위젯 (`HeaderClock`)**:
  - 헤더 우측 상단 실시간 디지털 시계 및 12시간/24시간제 전환 토글 스위치.
- [ ] **종합 도움말 & 가이드 모달 (`HelpModal`)**:
  - 헤더 물음표(`?`) 아이콘 및 단축키(`?`, `Ctrl+/`) 지원.
  - 브라우저 시작 페이지 설정법, 별(⭐) 아이콘 활용법, 로컬스토리지 보안 안내 수록.
 
